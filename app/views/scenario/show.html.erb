<div class="row scenario-sections">
  <% @scenario.each do |key, value| %>
    <div class="col-xs-10 col-xs-offset-1 <%= column_width_string %>">
      <%= render partial: 'scenario_section', locals: { title: key, items: value } %>
    </div>
  <% end %>
</div>