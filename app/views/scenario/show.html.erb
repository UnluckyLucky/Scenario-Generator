<%= render partial: 'columns', locals: { column_type: :regular } %>

<% if @spoiler_alert_display %>
  <%= render 'layouts/spoiler_alert' %>
<% end %>

<%= render partial: 'columns', locals: { column_type: :spoiler } %>
