<div id="sidebar-wrapper">
    <ul class="sidebar-nav">
        <li class="menu-toggle">
            <a href="#">Close Menu</a>
        </li>
        <% @games.each do |key, value| %>
            <li>
                <%= link_to value[:title], generator_path(game: key) + '#menu' %>
            </li>
        <% end %>
    </ul>
</div>